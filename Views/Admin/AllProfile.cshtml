@*@model IEnumerable<FishingTournament02.Models.Profile>

<p><a asp-controller="Admin" asp-action="AddProfile">Add a profile</a></p>

@{
    if (Model.Count() < 1)
    {
        <p>No profile in database yet.</p>
    }
    else
    {
        <h3>All Profiles</h3>
        <div class="table-responsive">
            <table class="table table-striped table-hover">

                <tr>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Address</th>
                    <th>Telephone</th>


                </tr>
                @foreach (var profiles in Model)
                {
            <tr>
                <td>@profiles.FirstName</td>
                <td>@profiles.LastName</td>
                <td>@profiles.Address</td>
                <td>@profiles.Telephone</td>


            </tr>
                }
            </table>
        </div>

    }
}*@

